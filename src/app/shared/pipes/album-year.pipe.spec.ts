import { AlbumYearPipe } from './album-year.pipe';

describe('AlbumYearPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumYearPipe();
    expect(pipe).toBeTruthy();
  });
});
